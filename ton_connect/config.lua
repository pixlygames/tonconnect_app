return {
    -- TON API key for NFT verification
    tonApiKey = "YOUR_TON_API_KEY_HERE", -- Replace with your actual TON API key
    
    -- Verification settings
    verificationInterval = 10, -- minutes between NFT ownership checks
    
    -- Notification settings
    notifyOnRevoke = true, -- whether to notify players when assets are revoked
    
    -- Debug settings
    debug = false, -- enable debug logging
}
